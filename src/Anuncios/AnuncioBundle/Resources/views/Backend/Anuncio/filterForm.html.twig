{% form_theme form 'GeckoBackendBundle::filterForms.html.twig' %}

<form method="get" action="{{ path('anuncios_anuncio_backend_anuncio_index') }}" class="form-inline form-filter">
    {{ form_row(form.name) }}
    {{ form_row(form.campaign) }}
    {{ form_row(form.category) }}
    <button type="submit" class="btn btn-primary"><i class="icon-search"></i> Buscar</button>
</form>
