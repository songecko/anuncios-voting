{{ form_errors(form) }}
<div class="row">
    <div class="col-md-6 row-split">
		{{ form_row(form.name) }}
    </div>
    <div class="col-md-6 row-split">
		{{ form_row(form.sectorId) }}
    </div>
</div>
{{ form_rest(form) }}
