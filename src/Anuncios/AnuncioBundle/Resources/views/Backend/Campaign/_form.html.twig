{{ form_errors(form) }}
<div class="row">
    <div class="col-md-6 row-split">
		{{ form_row(form.name) }}
		<br>
        {{ form_row(form.isActive) }}
    </div>
    
    <div class="col-md-6">
        {{ form_row(form.file) }}
    </div>
</div>
{{ form_rest(form) }}
