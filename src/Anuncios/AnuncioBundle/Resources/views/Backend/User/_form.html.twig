{{ form_errors(form) }}
<div class="row">
    <div class="col-md-6 row-split">
		{{ form_row(form.username) }}
    </div>
    <div class="col-md-6 row-split">
		{{ form_row(form.email) }}
    </div>
    <div class="col-md-6 row-split">
		{{ form_row(form.isjurado) }}
    </div>
</div>
{{ form_rest(form) }}
